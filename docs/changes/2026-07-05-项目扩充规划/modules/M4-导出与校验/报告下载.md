# 报告下载

上级：[[_M4-导出与校验]]
下级：无
依赖：报告 metadata

---

## 场景

生成后下载 Markdown 或 Word。

## 触发

GET `/api/reports/{report_id}/draft` 或 `/api/reports/{report_id}/files/report.docx`。

## 逻辑

- 校验 report_id 只在当前 user workspace 下。
- 文件存在则返回 FileResponse。
- 不存在返回 404。
