# 模板注册表

上级：[[_M1-模板系统]]
下级：无
依赖：无

---

## 场景

用户生成报告前需要选择模板；系统也需要知道每种报告的默认模板。

## 触发

后端启动或 API 请求读取模板配置。

## 逻辑

- 从 `templates/report_templates.json` 读取模板元数据。
- 每条包含 `id/type/name/path/variables/default`。
- 没有配置时提供内置默认模板。

## 状态 / 边界

- 模板文件不存在：启动不崩溃，但该模板不可用。
- 同类型多个 default：只取第一个并在校验里提示。
